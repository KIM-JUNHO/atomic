import { Icon } from 'components/Icon';
import { FirewallIcon } from 'components/Icon/FirewallIcon';

export default {
  title: 'Components/Icon',
  component: Icon,
};

export const Firewall = (args) => <FirewallIcon {...args} />;
